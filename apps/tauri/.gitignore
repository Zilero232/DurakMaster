# Сборка Cargo
/target/

# Генерируется Tauri: проект Android и схемы capabilities.
# Восстанавливается через `bun android:init` / `bun tauri build`.
/gen/

# Генерируются из apps/client/app/icon.svg через `bun tauri:icon`
# (см. icon.manifest.json).
/icons/

# Подпись релиза — в репозиторий не попадает никогда.
*.keystore
keystore.properties
