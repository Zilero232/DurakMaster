{
  "name": "@durak-master/tauri",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "icon": "tauri icon icon.manifest.json -o icons",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build --apk",
    "android:build:aab": "tauri android build --aab",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev",
    "ios:build": "tauri ios build",
    "info": "tauri info",
    "signer:generate": "tauri signer generate -w ~/.tauri/durakmaster.key --ci",
    "signer:sign": "tauri signer sign"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
