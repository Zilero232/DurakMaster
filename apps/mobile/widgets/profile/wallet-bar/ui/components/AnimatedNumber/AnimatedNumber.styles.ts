import { StyleSheet } from 'react-native';

import { colors, fontFamily } from '@/ui-kit';

export const styles = StyleSheet.create({
  root: {
    padding: 0,
    fontWeight: '700',
    fontFamily: fontFamily.sansBold,
    color: colors.foreground
  }
});
