# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Deps
node_modules
*.local

# Build output
dist
build
generated

# Expo
.expo
expo-env.d.ts

# Нативные проекты: создаются `expo prebuild` из app.json.
# Правки в них теряются при пересоздании — конфиг живёт в app.json и плагинах.
apps/mobile/android
apps/mobile/ios

# TypeScript incremental build info (can appear in any package)
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets — kept at root so they are caught in every package.
# The only env file committed is the template `.env.example`.
.env
.env.local
.env.*.local
!.env.example

# Android signing
*.keystore
*.jks

# Firebase (FCM service account / client config)
google-services.json
GoogleService-Info.plist
firebase-service-account.json

# Superpowers brainstorming artifacts (companion mockups, session state, specs)
.superpowers/

# Uploaded avatars
uploads
